diff --git a/Makefile.in b/Makefile.in
index d8e70d9f..ebd2a4dc 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -113,6 +113,6 @@ SKOBJS=ssh-sk.o sk-usbhid.o
 
 SSHOBJS= ssh.o readconf.o clientloop.o sshtty.o \
-	sshconnect.o sshconnect2.o mux.o ssh-pkcs11.o $(SKOBJS)
+	sshconnect.o sshconnect2.o mux.o ssh-pkcs11.o ohos-pwd.o $(SKOBJS)
 
 SSHDOBJS=sshd.o \
 	platform-listen.o \
@@ -154,4 +154,4 @@ SFTP_CLIENT_OBJS=sftp-common.o sftp-client.o sftp-glob.o ssherr-nolibcrypto.o
 
-SCP_OBJS=	scp.o progressmeter.o $(SFTP_CLIENT_OBJS)
+SCP_OBJS=	scp.o progressmeter.o ohos-pwd.o $(SFTP_CLIENT_OBJS)
 
 SSHADD_OBJS=	ssh-add.o $(P11OBJS) $(SKOBJS)
diff --git a/ohos-pwd.c b/ohos-pwd.c
new file mode 100644
index 00000000..98cdb2cb
--- /dev/null
+++ b/ohos-pwd.c
@@ -0,0 +1,39 @@
+/* HarmonyOS application UIDs do not have entries in /etc/passwd. */
+
+#include "includes.h"
+
+#include <sys/types.h>
+
+#include <pwd.h>
+#include <stdlib.h>
+#include <unistd.h>
+
+#include "ohos-pwd.h"
+
+static char *
+environment_or_default(const char *name, char *fallback)
+{
+	char *value = getenv(name);
+
+	return value != NULL && *value != '\0' ? value : fallback;
+}
+
+struct passwd *
+ohos_getpwuid(void)
+{
+	static char default_name[] = "zed";
+	static char default_password[] = "*";
+	static char default_home[] = "/data/app";
+	static char default_shell[] = "/system/bin/sh";
+	static char default_gecos[] = "Zed";
+	static struct passwd user;
+
+	user.pw_name = environment_or_default("USER", default_name);
+	user.pw_passwd = default_password;
+	user.pw_uid = getuid();
+	user.pw_gid = getgid();
+	user.pw_gecos = default_gecos;
+	user.pw_dir = environment_or_default("HOME", default_home);
+	user.pw_shell = environment_or_default("SHELL", default_shell);
+	return &user;
+}
diff --git a/ohos-pwd.h b/ohos-pwd.h
new file mode 100644
index 00000000..ba1cbe3d
--- /dev/null
+++ b/ohos-pwd.h
@@ -0,0 +1,8 @@
+#ifndef OHOS_PWD_H
+#define OHOS_PWD_H
+
+#include <pwd.h>
+
+struct passwd *ohos_getpwuid(void);
+
+#endif
diff --git a/scp.c b/scp.c
index d5178366..2097cb9b 100644
--- a/scp.c
+++ b/scp.c
@@ -119,6 +119,7 @@
 
 #include "sftp-common.h"
 #include "sftp-client.h"
+#include "ohos-pwd.h"
 
 extern char *__progname;
 
@@ -508,6 +509,8 @@ main(int argc, char **argv)
 		mode = MODE_SCP;
 
 	if ((pwd = getpwuid(userid = getuid())) == NULL)
+		pwd = ohos_getpwuid();
+	if (pwd == NULL)
 		fatal("unknown user %u", (u_int) userid);
 
 	if (!isatty(STDOUT_FILENO))
diff --git a/ssh.c b/ssh.c
index 3bf3a176..782025e9 100644
--- a/ssh.c
+++ b/ssh.c
@@ -92,6 +92,7 @@
 #include "version.h"
 #include "ssherr.h"
 #include "utf8.h"
+#include "ohos-pwd.h"
 
 #ifdef ENABLE_PKCS11
 #include "ssh-pkcs11.h"
@@ -703,6 +704,8 @@ main(int ac, char **av)
 
 	/* Get user data. */
 	pw = getpwuid(getuid());
+	if (pw == NULL)
+		pw = ohos_getpwuid();
 	if (!pw) {
 		logit("No user exists for uid %lu", (u_long)getuid());
 		exit(255);
